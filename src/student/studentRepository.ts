// src/student/studentRepository.ts
import { Student } from './studentModel';

export class StudentRepository {
    async registerStudent(student: Student) {
        // Lógica para comunicarse con el backend y registrar un estudiante
    }
}
