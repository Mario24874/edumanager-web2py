// src/student/studentRenderer.ts
import { Student } from './studentModel';

export class StudentRenderer {
    render(student: Student) {
        // Lógica para generar el HTML del formulario de registro de estudiantes
    }
}
