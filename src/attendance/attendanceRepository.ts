// src/attendance/attendanceRepository.ts
import { Attendance } from './attendanceModel';

export class AttendanceRepository {
    async registerAttendance(attendance: Attendance) {
        // Lógica para comunicarse con el backend y registrar la asistencia
    }
}
