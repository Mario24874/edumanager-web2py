// src/attendance/attendanceRenderer.ts
import { Attendance } from './attendanceModel';

export class AttendanceRenderer {
    render(attendance: Attendance) {
        // Lógica para generar el HTML del formulario de registro de asistencia
    }
}
