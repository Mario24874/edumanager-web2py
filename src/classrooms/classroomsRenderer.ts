// src/classroom/classroomRenderer.ts
import { Classrooms } from './classroomsModel';

export class ClassroomsRenderer {
    render(classrooms: Classrooms) {
        // Lógica para generar el HTML del formulario de creación de salones
    }
}

// src/subject/subjectRenderer.ts
import { Subjects } from './subjectsModel';

export class SubjectsRenderer {
    render(subjects: Subjects) {
        // Lógica para generar el HTML del formulario de creación de materias
    }
}
