// src/classroom/classroomRepository.ts
import { Classrooms } from './classroomsModel';

export class ClassroomsRepository {
    async createClassrooms(classrooms: Classrooms) {
        // Lógica para comunicarse con el backend y crear un salón
    }
}

// src/subject/subjectRepository.ts
import { Subjects } from './subjectsModel';

export class SubjectsRepository {
    async createSubjects(subjects: Subjects) {
        // Lógica para comunicarse con el backend y crear una materia
    }
}
